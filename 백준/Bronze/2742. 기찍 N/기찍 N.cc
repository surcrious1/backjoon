#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<ctype.h>
#include<math.h>
#include<stack>

using namespace std;

int main(void) {
	int n;
	scanf("%d", &n);
	while (n != 0) {
		printf("%d\n", n);
		n--;
	}
	return 0;
}